# ===== Frontend 環境変数サンプル =====
# NEXT_PUBLIC_ 接頭辞の値はブラウザに露出します。秘密情報は入れないこと。

# バックエンド API のベースURL
NEXT_PUBLIC_API_URL=http://localhost:5021

# Google ログイン用 OAuth クライアントID（未設定なら Google ボタン非表示）
# バックエンドの GOOGLE_CLIENT_ID と同じ値を設定すること。
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
